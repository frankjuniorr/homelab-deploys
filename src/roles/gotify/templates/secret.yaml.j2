apiVersion: v1
kind: Secret
metadata:
  name: {{ app.name }}-env
  namespace: {{ app.namespace }}
type: Opaque
stringData:
  GOTIFY_DEFAULTUSER_NAME: "admin"
  GOTIFY_DEFAULTUSER_PASS: "{{ gotify_admin_password }}"
