apiVersion: v1
kind: Service
metadata:
  name: {{ app.name }}-svc
  namespace: {{ app.namespace }}
spec:
  selector:
    app: {{ app.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9898
