apiVersion: v1
kind: Secret
metadata:
  name: {{ longhorn_backup_credential_secret }}
  namespace: {{ longhorn_namespace }}
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "{{ garage_s3_access_key }}"
  AWS_SECRET_ACCESS_KEY: "{{ garage_s3_secret_key }}"
  AWS_ENDPOINTS: "{{ garage_s3_endpoint }}"
