apiVersion: v1
kind: Secret
metadata:
  name: cnpg-backup-secret
  namespace: {{ namespace_postgres }}
type: Opaque
stringData:
  ACCESS_KEY_ID: "{{ garage_s3_access_key }}"
  ACCESS_SECRET_KEY: "{{ garage_s3_secret_key }}"
