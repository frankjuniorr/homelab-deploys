---
##############################################
# MONITORING ROLE
# Deploys the full observability stack across dedicated namespaces:
#   - kube-prometheus-stack: Prometheus + Alertmanager + kube-state-metrics + node-exporter (ns: prometheus)
#   - Grafana:               Standalone dashboards + datasources (ns: grafana)
#   - Loki:                  Log aggregation (single binary mode) (ns: loki)
#   - Promtail:              DaemonSet log collector — ships pod logs to Loki (ns: loki)
#
# Grafana     → grafana.frank.lab.io     (service: grafana:80)
# Prometheus  → prometheus.frank.lab.io  (service: kube-prometheus-stack-prometheus:9090)
# Loki        → internal only            (service: loki:3100)
#
# Grafana admin password: vault_grafana_admin_password (vault.yml) — add via: just secrets-edit
# Loki datasource is pre-configured in Grafana via grafana-values.yaml.j2
##############################################

- name: "Add Helm repos (prometheus-community + grafana)"
  ansible.builtin.command:
    cmd: "helm repo add {{ item.name }} {{ item.url }} --force-update"
  loop:
    - { name: prometheus-community, url: "https://prometheus-community.github.io/helm-charts" }
    - { name: grafana, url: "https://grafana.github.io/helm-charts" }
  loop_control:
    label: "{{ item.name }}"
  changed_when: false

- name: "Update Helm repo cache"
  ansible.builtin.command:
    cmd: helm repo update
  changed_when: false

- name: "Write Helm values to temp files"
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "0600"
  loop:
    - { src: kube-prometheus-stack-values.yaml.j2, dest: /tmp/kube-prometheus-stack-values.yaml }
    - { src: loki-values.yaml.j2,                  dest: /tmp/loki-values.yaml }
    - { src: grafana-values.yaml.j2,               dest: /tmp/grafana-values.yaml }
  loop_control:
    label: "{{ item.src }}"

- name: "Install kube-prometheus-stack (Prometheus + Alertmanager + kube-state-metrics)"
  ansible.builtin.command:
    cmd: >
      helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack
      --namespace {{ namespace_prometheus }}
      --create-namespace
      --version {{ kube_prometheus_stack_version }}
      --wait
      --values /tmp/kube-prometheus-stack-values.yaml
  register: helm_kps
  changed_when: "'has been upgraded' in helm_kps.stdout or 'has been installed' in helm_kps.stdout"

- name: "Install Grafana (standalone — dashboards + datasources)"
  ansible.builtin.command:
    cmd: >
      helm upgrade --install grafana grafana/grafana
      --namespace {{ namespace_grafana }}
      --create-namespace
      --version {{ grafana_version }}
      --wait
      --values /tmp/grafana-values.yaml
  register: helm_grafana
  changed_when: "'has been upgraded' in helm_grafana.stdout or 'has been installed' in helm_grafana.stdout"

- name: "Install Loki (log aggregation, single binary + filesystem storage)"
  ansible.builtin.command:
    cmd: >
      helm upgrade --install loki grafana/loki
      --namespace {{ namespace_loki }}
      --create-namespace
      --version {{ loki_version }}
      --wait
      --values /tmp/loki-values.yaml
  register: helm_loki
  changed_when: "'has been upgraded' in helm_loki.stdout or 'has been installed' in helm_loki.stdout"

- name: "Install Promtail (log collector — ships pod logs to Loki)"
  ansible.builtin.command:
    cmd: >
      helm upgrade --install promtail grafana/promtail
      --namespace {{ namespace_loki }}
      --create-namespace
      --version {{ promtail_version }}
      --wait
      --set config.clients[0].url=http://loki.{{ namespace_loki }}.svc.cluster.local:3100/loki/api/v1/push
  register: helm_promtail
  changed_when: "'has been upgraded' in helm_promtail.stdout or 'has been installed' in helm_promtail.stdout"

- name: "Remove temporary Helm values files"
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - /tmp/kube-prometheus-stack-values.yaml
    - /tmp/loki-values.yaml
    - /tmp/grafana-values.yaml
  loop_control:
    label: "{{ item }}"

- name: "Apply Grafana dashboard ConfigMaps (Loki log monitoring)"
  kubernetes.core.k8s:
    state: present
    src: "{{ role_path }}/files/{{ item }}"
  loop:
    - grafana-dashboard-cluster-logs.yaml
    - grafana-dashboard-pod-logs.yaml
  loop_control:
    label: "{{ item }}"

- name: "Apply monitoring HTTPRoutes (Grafana + Prometheus)"
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', role_path + '/templates/' + item.file) | from_yaml }}"
  loop:
    - file: grafana-httproute.yaml.j2
    - file: prometheus-httproute.yaml.j2
  loop_control:
    label: "{{ item.file }}"
